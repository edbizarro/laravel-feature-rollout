{
    "name": "edbizarro/laravel-feature-rollout",
    "description": "Feature rollout in your Laravel application.",
    "keywords": [
        "laravel",
        "rollout",
        "feature-rollout",
        "feature-toggle"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Eduardo Bizarro",
            "email": "eduardo@zendev.com.br"
        }
    ],
    "require": {
        "php" : "~7.0"
    },
    "require-dev": {
        "mockery/mockery": "^0.9.9",
        "orchestra/testbench": "~3.0",
        "phpunit/phpunit": "^6.3",
        "squizlabs/php_codesniffer": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Edbizarro\\FeatureRollout\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Edbizarro\\FeatureRollout\\Test": "tests"
        }
    },
    "scripts": {
        "test": "phpunit",
        "check-style": "phpcs -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src tests",
        "fix-style": "phpcbf -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src tests"
    },
    "config": {
        "sort-packages": true
    },
    "prefer-stable": true,
    "minimum-stability": "dev"
}
